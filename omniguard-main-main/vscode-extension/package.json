{
  "name": "omniguard",
  "displayName": "OmniGuard Security",
  "description": "AI-powered application security scanner — inline diagnostics, secret detection, SAST, and AI remediation",
  "version": "2.1.2",
  "publisher": "omniguard",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/Suryavamsiram/omniguard-enterprise"
  },
  "engines": { "vscode": "^1.80.0" },
  "categories": ["Linters", "Other"],
  "keywords": ["security", "secrets", "sast", "appsec", "omniguard"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        { "id": "omniguard", "title": "OmniGuard Security", "icon": "$(shield)" }
      ]
    },
    "views": {
      "omniguard": [
        { "id": "omniguardFindings", "name": "Security Findings" }
      ]
    },
    "commands": [
      { "command": "omniguard.scanFile",          "title": "OmniGuard: Scan Current File" },
      { "command": "omniguard.scanWorkspace",     "title": "OmniGuard: Scan Workspace" },
      { "command": "omniguard.configure",         "title": "OmniGuard: Configure / Login" },
      { "command": "omniguard.clearDiagnostics",  "title": "OmniGuard: Clear Diagnostics" },
      { "command": "omniguard.showFindings",      "title": "OmniGuard: Show Findings Panel" },
      { "command": "omniguard.nexusGraph",        "title": "OmniGuard: Show Architecture Nexus" },
      { "command": "omniguard.agentMap",          "title": "OmniGuard: Run System Mapping Agent" },
      { "command": "omniguard.explain",           "title": "OmniGuard: Explain Finding" },
      { "command": "omniguard.fixFinding",        "title": "OmniGuard: Fix Finding (AI Remediation)" },
      { "command": "omniguard.createJira",        "title": "OmniGuard: Create Jira Ticket" },
      { "command": "omniguard.createServiceNow",  "title": "OmniGuard: Create ServiceNow Incident" },
      { "command": "omniguard.showLogs",          "title": "OmniGuard: Show Output Logs" }
    ],
    "configuration": {
      "title": "OmniGuard",
      "properties": {
        "omniguard.supabaseUrl": {
          "type": "string",
          "default": "",
          "description": "Supabase project Functions URL (e.g. https://id.supabase.co/functions/v1)"
        },
        "omniguard.apiKey": {
          "type": "string",
          "default": "",
          "description": "OmniGuard API key from Dashboard → Settings → API Keys"
        },
        "omniguard.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the omniguard CLI binary or index.js (leave blank to auto-detect)"
        },
        "omniguard.daemonPort": {
          "type": "number",
          "default": 5175,
          "description": "Port that the OmniGuard local daemon listens on"
        },
        "omniguard.scanTimeout": {
          "type": "number",
          "default": 30000,
          "description": "Maximum time in ms to wait for a CLI scan to complete before aborting"
        },
        "omniguard.enableOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Scan files automatically on save"
        },
        "omniguard.enableOnType": {
          "type": "boolean",
          "default": false,
          "description": "Scan files while typing (debounced, may be slow)"
        },
        "omniguard.failOnSeverity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low", "info"],
          "default": "high",
          "description": "Minimum severity to show as an error (vs warning)"
        },
        "omniguard.scanDelay": {
          "type": "number",
          "default": 800,
          "description": "Debounce delay in ms before scanning after save"
        },
        "omniguard.excludePatterns": {
          "type": "array",
          "default": ["**/node_modules/**", "**/dist/**", "**/build/**", "**/.git/**"],
          "description": "Glob patterns to exclude from scanning"
        }
      }
    },
    "menus": {
      "editor/context": [
        {
          "command": "omniguard.scanFile",
          "group": "omniguard@1",
          "when": "editorTextFocus"
        },
        {
          "command": "omniguard.showFindings",
          "group": "omniguard@2",
          "when": "editorTextFocus"
        }
      ],
      "explorer/context": [
        {
          "command": "omniguard.scanWorkspace",
          "group": "omniguard"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "vsce package --no-yarn",
    "publish-ext": "vsce publish --no-yarn"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0"
  }
}
